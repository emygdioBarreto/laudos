server:
  port: 8081
  servlet:
    context-path: /api
  forward-headers-strategy: framework

spring:
  application:
    name: laudos-local

  datasource:
    url: jdbc:postgresql://localhost:5432/dbLaudo
    username: postgres
    password: xyku
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

# CONFIGURAÇÃO DE VALIDAÇÃO (NOVO)
app:
  urlValidacao: http://192.168.255.102:8084/certificados/
